backend = "glx";
glx-no-stencil = true;
vsync = true;

opacity-rule = [
    "90:class_g = 'URxvt' && focused",
    "90:class_g = 'Polybar'"
];

inactive-window-opacity = 1;
inactive-opacity = 0.8;
use-ewmh-active-win = true;
